name: Build And Test On Commit

on:
  push:
    branches:
      - main
      - dev

jobs:
  build:
    runs-on: ubuntu-latest
    container:
      image: golang
    steps:
      - run: go mod download
      - run: make build